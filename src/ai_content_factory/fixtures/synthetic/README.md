# Synthetic fixtures

These files are offline-only inputs for the phase-one demo. They contain no
network URLs, credentials, private brand paths, real media bytes, or live
provider configuration. The providers return deterministic text and
placeholder media descriptors so the pipeline and publishing boundaries can be
tested without external accounts or services.
